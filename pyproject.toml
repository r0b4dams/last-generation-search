[project]
name = "letztegensuchen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Robert Adams", email = "r0b4dams@gmail.com" }]
requires-python = ">=3.12"
dependencies = ["beautifulsoup4>=4.13.3", "requests>=2.32.3"]

[project.scripts]
letztegen = "letztegensuchen:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["ruff>=0.9.5"]
